import fs from 'node:fs';

const files = {
  state: 'docs/PROJECT_STATE.md',
  roadmap: 'docs/ROADMAP.md',
  changelog: 'docs/CHANGELOG.md',
  spec: 'docs/keystone/specs/2026-08-05-portfolio-1-0-evidence-first.md',
};

function replaceOnce(text, from, to, label) {
  const first = text.indexOf(from);
  if (first < 0) throw new Error(`missing patch anchor: ${label}`);
  if (text.indexOf(from, first + from.length) >= 0) throw new Error(`ambiguous patch anchor: ${label}`);
  return text.slice(0, first) + to + text.slice(first + from.length);
}

function patch(path, transform) {
  const before = fs.readFileSync(path, 'utf8');
  const after = transform(before);
  if (after === before) throw new Error(`patch produced no change: ${path}`);
  fs.writeFileSync(path, after, 'utf8');
}

const sha = '17aa2cc5dd13b38ebd83f15d7596d8216f9d8b87';
const acceptance = `### P3.5A exact production acceptance\n\nP3.5A добавил controlled English Vlezet flagship без второго lifecycle/evidence source of truth. English presentation получает status и Project Evidence из тех же canonical registries; volatile heads/run IDs не дублируются в narrative prose.\n\n\`\`\`text\nPR #148 squash / deployed SHA:  ${sha}\nBuild:                          #1081 / 31153441505 — SUCCESS\nunit tests:                     428 PASS / 0 FAIL\nquality artifact:               8984294852\nquality digest:                 sha256:31615e8b6240535e653bf618cc913d00515675636ddfc2ecff7ed11b3ec50b2f\nCodeQL:                         #581 / 31153441477 — SUCCESS\nDependency Review:              #509 / 31153441537 — SUCCESS\nDistribution Readiness:         #95 / 31153441483 — SUCCESS\nPages:                          #177 / 31155442788 — SUCCESS\nPages deployment ID:            5790177102\nPages artifact:                 8984956581\nPages artifact digest:          sha256:aba35d85b288676c6d284469145a4108eba97123acface36de6a0b1df26ecd82\nProduction Live Smoke:          #214 / 31155442779 — SUCCESS\nproduction artifact:            8985006008\nproduction digest:              sha256:a91b35963c685068c6ee79aff269de3baa297d9a6b8fcc321a945e696db84784\npost-merge CodeQL:              #582 / 31155442796 — SUCCESS\n\`\`\`\n\nGitHub Advanced Security TOCTOU finding in Project Evidence file reads was reproduced with a RED contract, fixed by direct reads plus fail-closed \`ENOENT\` handling, and automatically resolved by the subsequent CodeQL scan. P3.5A is accepted only on the exact deployed squash SHA above.\n\n`;

patch(files.state, (input) => {
  let text = input;
  text = replaceOnce(text,
    '> Последнее смысловое обновление: **2026-08-06**, после exact production-acceptance Portfolio 1.0 P3.4F Evidence-driven Project State Note.',
    '> Последнее смысловое обновление: **2026-08-07**, после exact production-acceptance Portfolio 1.0 P3.5A English Vlezet flagship.',
    'state update date');
  text = replaceOnce(text,
    '**P3.4F — Evidence-driven project state**.\n\nProduction route:\n\n```text\n/landing/notes/evidence-driven-project-state/\n```',
    '**P3.5A — English Vlezet flagship**.\n\nProduction route:\n\n```text\n/en/projects/vlezet/\n```',
    'latest accepted milestone');
  text = replaceOnce(text, 'Принятая evidence model:', 'Inherited evidence model from P3.4F:', 'evidence model label');
  text = replaceOnce(text, '### Feature and operational PRs', '### P3.4F feature and operational evidence', 'P3.4F feature label');
  text = replaceOnce(text, '### Exact production acceptance', '### P3.4F exact production acceptance', 'P3.4F production label');
  text = replaceOnce(text,
    'P3.4F accepted только на exact deployed SHA `8d2c3aa45d2b02ad3c22de75aca3602b009c13e6`. Repository activity или generated bytes без deployment identity и independent Production Live Smoke не считаются эквивалентным доказательством.\n\n---',
    'P3.4F accepted только на exact deployed SHA `8d2c3aa45d2b02ad3c22de75aca3602b009c13e6`. Repository activity или generated bytes без deployment identity и independent Production Live Smoke не считаются эквивалентным доказательством.\n\n' + acceptance + '---',
    'state P3.5A acceptance insertion');
  text = replaceOnce(text,
    '**P3.5 — Selective English expansion**.\n\nСледующий bounded этап должен расширять английскую версию только для high-value surfaces: homepage, Resume, три flagship case studies, `/now`, выбранные Engineering Notes и Publications. Он обязан использовать ту же canonical registry/build/search architecture, не создавать отдельный English CMS и не переводить Draft или непроверенные external-project claims.',
    '**P3.5B — English /now — NEXT**.\n\nP3.5A уже закрыл третий flagship: `/en/projects/vlezet/`. Следующий bounded этап переводит `/now` из того же canonical data contract без отдельного English state registry. После него P3.5C отдельно покрывает Publications. Draft или непроверенные external-project claims не продвигаются.',
    'state next slice');
  return text;
});

patch(files.roadmap, (input) => {
  let text = input;
  text = replaceOnce(text,
    '> Обновлено: **2026-08-06**, после exact production-acceptance Portfolio 1.0 P3.4F Evidence-driven Project State Note.',
    '> Обновлено: **2026-08-07**, после exact production-acceptance Portfolio 1.0 P3.5A English Vlezet flagship.',
    'roadmap update date');
  text = replaceOnce(text,
    '- P3.4F Evidence-driven Project State Note and Pages recovery contract — PR #141–#145.',
    '- P3.4F Evidence-driven Project State Note and Pages recovery contract — PR #141–#145.\n- P3.5A English Vlezet flagship — PR #148.',
    'completed P3.5A');
  text = replaceOnce(text,
    '## P3.5 — Selective English expansion — NEXT\n\nTranslate only high-value surfaces: homepage, Resume, three flagships, `/now`, selected Notes and Publications. Do not create a separate English CMS, build or search architecture.',
    `## P3.5 — Selective English expansion — IN PROGRESS\n\n### P3.5A — English Vlezet flagship — DONE\n\nAccepted route: \`/en/projects/vlezet/\`. One RU/EN pair, canonical metadata, generated search, localized registry-derived Project Evidence, semantic no-JavaScript fallback and exact-deployment flagship smoke are accepted without a second lifecycle/evidence model.\n\n\`\`\`text\nPR #148 squash / deployed SHA:  ${sha}\nBuild:                          #1081 / 31153441505 — SUCCESS\nPages:                          #177 / 31155442788 — SUCCESS\nPages deployment ID:            5790177102\nProduction Live Smoke:          #214 / 31155442779 — SUCCESS\nproduction artifact:            8985006008\nproduction digest:              sha256:a91b35963c685068c6ee79aff269de3baa297d9a6b8fcc321a945e696db84784\n\`\`\`\n\n### P3.5B — English /now — NEXT\n\nAdd a controlled English \`/now\` surface from the same canonical now-data contract. Keep one updated date, one evidence boundary and one build/search architecture; do not create \`now-en.json\` or a second current-state model.\n\n### P3.5C — English Publications — PLANNED\n\nTranslate the Publications presentation as a separate bounded slice while preserving the same publication registry and single generated search.`,
    'roadmap P3.5 slices');
  text = replaceOnce(text,
    'Continue with **P3.5 — Selective English expansion**.',
    `Confirm P3.5A exact production acceptance for SHA \`${sha}\`, Pages run \`31155442788\`, deployment \`5790177102\` and Production Live run \`31155442779\`. Continue with **P3.5B — English /now**.`,
    'roadmap new-session rule');
  return text;
});

patch(files.spec, (input) => {
  let text = input;
  text = replaceOnce(text,
    '> Status: **IN PROGRESS — P3.4F ACCEPTED IN PRODUCTION**\n>\n> Date: **2026-08-06**',
    '> Status: **IN PROGRESS — P3.5A ACCEPTED IN PRODUCTION**\n>\n> Date: **2026-08-07**',
    'spec status');
  text = replaceOnce(text,
    'Continue with **P3.5 — Selective English expansion**.',
    `### P3.5A — English Vlezet flagship — DONE\n\nControlled route \`/en/projects/vlezet/\` reuses canonical Vlezet lifecycle and evidence registries. Presentation copy is localized, while volatile project identities remain registry-owned. Browser/no-JavaScript/search/metadata contracts and the exact-deployment flagship smoke passed without introducing a separate English CMS or search index.\n\n\`\`\`text\nPR #148 squash / deployed SHA:  ${sha}\nBuild:                          #1081 / 31153441505 — SUCCESS\nunit tests:                     428 PASS / 0 FAIL\nCodeQL:                         #581 / 31153441477 — SUCCESS\nPages:                          #177 / 31155442788 — SUCCESS\nPages deployment ID:            5790177102\nProduction Live Smoke:          #214 / 31155442779 — SUCCESS\nproduction artifact:            8985006008\nproduction digest:              sha256:a91b35963c685068c6ee79aff269de3baa297d9a6b8fcc321a945e696db84784\n\`\`\`\n\nThe CodeQL TOCTOU finding discovered during review was reproduced, fixed without behavior weakening and automatically resolved on the corrected exact head.\n\n### P3.5B — English /now — NEXT\n\nPublish English current-work context from the same canonical \`now\` data contract. Preserve one updated date, one acceptance boundary and one build/search architecture. P3.5C Publications remains a separate later slice.`,
    'spec P3.5 insertion');
  return text;
});

patch(files.changelog, (input) => {
  let text = input;
  text = replaceOnce(text,
    '> Обновлено: **2026-08-06**, после exact production-acceptance Portfolio 1.0 P3.4F Evidence-driven Project State Note.',
    '> Обновлено: **2026-08-07**, после exact production-acceptance Portfolio 1.0 P3.5A English Vlezet flagship.',
    'changelog update date');
  const anchor = '## 2026-08-06 — P3.4F Evidence-driven project state';
  const section = `## 2026-08-07 — P3.5A English Vlezet flagship\n\nPR #148 completed the third controlled English flagship at \`/en/projects/vlezet/\` without creating a second project-state model.\n\nAdded canonical RU/EN pairing, metadata/OpenGraph, navigation, generated search, localized registry-derived Project Evidence, semantic no-JavaScript fallback, mobile/accessibility coverage and exact-deployment flagship verification. Volatile heads/run identities remain owned by dated canonical evidence rather than article prose.\n\nGitHub Advanced Security found a potential check-then-read filesystem race in Project Evidence. A RED regression reproduced the class, direct reads with fail-closed \`ENOENT\` handling removed it, and CodeQL automatically resolved the review thread on the corrected head.\n\n\`\`\`text\nPR #148 squash / deployed SHA:  ${sha}\nBuild:                          #1081 / 31153441505 — SUCCESS\nunit tests:                     428 PASS / 0 FAIL\nquality artifact:               8984294852\nquality digest:                 sha256:31615e8b6240535e653bf618cc913d00515675636ddfc2ecff7ed11b3ec50b2f\nCodeQL:                         #581 / 31153441477 — SUCCESS\nDependency Review:              #509 / 31153441537 — SUCCESS\nDistribution Readiness:         #95 / 31153441483 — SUCCESS\nPages:                          #177 / 31155442788 — SUCCESS\nPages deployment ID:            5790177102\nProduction Live Smoke:          #214 / 31155442779 — SUCCESS\nproduction artifact:            8985006008\nproduction digest:              sha256:a91b35963c685068c6ee79aff269de3baa297d9a6b8fcc321a945e696db84784\npost-merge CodeQL:              #582 / 31155442796 — SUCCESS\n\`\`\`\n\nNext bounded slice: **P3.5B — English /now — NEXT**. Publications remains P3.5C.\n\n`;
  text = replaceOnce(text, anchor, section + anchor, 'changelog P3.5A insertion');
  return text;
});

for (const path of Object.values(files)) {
  const text = fs.readFileSync(path, 'utf8');
  if (!text.includes(sha)) throw new Error(`accepted SHA missing after patch: ${path}`);
  if (!text.includes('P3.5A')) throw new Error(`P3.5A missing after patch: ${path}`);
}

fs.rmSync('scripts/_p3-5a-state-patch.mjs');
fs.rmSync('.github/workflows/_p3-5a-state-patch.yml');
